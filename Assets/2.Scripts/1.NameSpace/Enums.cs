using UnityEngine;

namespace EtudeProject
{
    public enum CharacterStateType
    {
        Idle,
        Move,
        Attack,
        Dead
    }

    public enum Direction
    {
       Horizontal = 0,
       Vertical = 1,
    }
}
