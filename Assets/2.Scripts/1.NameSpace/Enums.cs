using UnityEngine;

namespace EtudeProject
{
    public enum CharacterStateType
    {
        Idle,
        Move,
        Attack,
        Dead
    }

    public enum StageFloor
    {
       Floor1 = 0,
       Floor2 = 1,
       Floor3 = 2,

    }
}
